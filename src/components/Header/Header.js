import React from "react";
import "./Header.scss";

const Header = () => {
  return (
    <React.Fragment>
      <h1 class="header">Tap Room</h1>
    </React.Fragment>
  );
};

export default Header;
